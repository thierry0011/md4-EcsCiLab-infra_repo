AWSTemplateFormatVersion: '2010-09-09'
Description: >
  ECS CI/CD Lab - GitHub Actions OIDC federation for the infra repo's on-demand
  cascade-resync workflow. Same shape as 06-github-oidc.yaml (which does this for
  the app repo), but for a different actor with a much wider blast radius: this
  role runs `aws cloudformation deploy`/change-sets across all seven stacks, not
  just an ECR push. GitSync's own per-file auto-deploy trigger is left untouched
  by this - this role is only assumed by the separate, manually-triggered
  deploy-infra.yml workflow that resyncs every stack on demand.

Parameters:
  EnvironmentName:
    Type: String
    Default: ecs-cicd-lab
  GitHubOrg:
    Type: String
    Description: GitHub org or username that owns the infra repo
  GitHubInfraRepo:
    Type: String
    Description: Name of the infrastructure repository (not org/repo, just the repo name)
  GitHubBranch:
    Type: String
    Default: main
    Description: Branch allowed to assume this role
  CreateOidcProvider:
    Type: String
    Default: 'false'
    AllowedValues: ['true', 'false']
    Description: >
      Leave false if 06-github-oidc.yaml (deployed earlier in the standard
      deployment order) already created the account's one OIDC provider for
      token.actions.githubusercontent.com.

Conditions:
  ShouldCreateProvider: !Equals [!Ref CreateOidcProvider, 'true']

Resources:
  GitHubOidcProvider:
    Type: AWS::IAM::OIDCProvider
    Condition: ShouldCreateProvider
    Properties:
      Url: https://token.actions.githubusercontent.com
      ClientIdList:
        - sts.amazonaws.com
      ThumbprintList:
        - 6938fd4d98bab03faadb97b34396831e3780aea1

  InfraDeployRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${EnvironmentName}-infra-deploy-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Federated: !If
                - ShouldCreateProvider
                - !Ref GitHubOidcProvider
                - !Sub arn:aws:iam::${AWS::AccountId}:oidc-provider/token.actions.githubusercontent.com
            Action: sts:AssumeRoleWithWebIdentity
            Condition:
              StringEquals:
                token.actions.githubusercontent.com:aud: sts.amazonaws.com
              StringLike:
                token.actions.githubusercontent.com:sub: !Sub repo:${GitHubOrg}/${GitHubInfraRepo}:ref:refs/heads/${GitHubBranch}
      Policies:
        - PolicyName: cascade-resync-permissions
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              # ---- CloudFormation: create/inspect/execute change sets on the
              # seven lab stacks only, never on anything else in the account ----
              - Sid: CloudFormationChangeSets
                Effect: Allow
                Action:
                  - cloudformation:CreateChangeSet
                  - cloudformation:ExecuteChangeSet
                  - cloudformation:DescribeChangeSet
                  - cloudformation:DeleteChangeSet
                  - cloudformation:DescribeStacks
                  - cloudformation:DescribeStackEvents
                Resource: !Sub arn:aws:cloudformation:${AWS::Region}:${AWS::AccountId}:stack/${EnvironmentName}-*/*

              # ---- EC2: 01-network.yaml (VPC/subnets/NAT/routes) and
              # 03-security-endpoints.yaml (SGs/VPC endpoints). Most EC2 create/
              # describe actions don't support resource-level ARNs, hence '*' -
              # scoped by action list instead of by Resource. ----
              - Sid: Ec2AvailabilityZoneLookup
                Effect: Allow
                Action: ec2:DescribeAvailabilityZones
                Resource: '*'
              - Sid: Ec2NetworkAndSecurity
                Effect: Allow
                Action:
                  - ec2:CreateVpc
                  - ec2:DeleteVpc
                  - ec2:DescribeVpcs
                  - ec2:ModifyVpcAttribute
                  - ec2:CreateSubnet
                  - ec2:DeleteSubnet
                  - ec2:DescribeSubnets
                  - ec2:ModifySubnetAttribute
                  - ec2:CreateInternetGateway
                  - ec2:DeleteInternetGateway
                  - ec2:AttachInternetGateway
                  - ec2:DetachInternetGateway
                  - ec2:DescribeInternetGateways
                  - ec2:CreateRouteTable
                  - ec2:DeleteRouteTable
                  - ec2:CreateRoute
                  - ec2:DeleteRoute
                  - ec2:AssociateRouteTable
                  - ec2:DisassociateRouteTable
                  - ec2:ReplaceRouteTableAssociation
                  - ec2:DescribeRouteTables
                  - ec2:AllocateAddress
                  - ec2:ReleaseAddress
                  - ec2:DescribeAddresses
                  - ec2:CreateNatGateway
                  - ec2:DeleteNatGateway
                  - ec2:DescribeNatGateways
                  - ec2:CreateSecurityGroup
                  - ec2:DeleteSecurityGroup
                  - ec2:DescribeSecurityGroups
                  - ec2:AuthorizeSecurityGroupIngress
                  - ec2:AuthorizeSecurityGroupEgress
                  - ec2:RevokeSecurityGroupIngress
                  - ec2:RevokeSecurityGroupEgress
                  - ec2:CreateVpcEndpoint
                  - ec2:DeleteVpcEndpoints
                  - ec2:ModifyVpcEndpoint
                  - ec2:DescribeVpcEndpoints
                  - ec2:CreateTags
                  - ec2:DeleteTags
                  - ec2:DescribeTags
                Resource: '*'

              # ---- ECR: 02-ecr.yaml ----
              - Sid: EcrRepository
                Effect: Allow
                Action:
                  - ecr:CreateRepository
                  - ecr:DeleteRepository
                  - ecr:DescribeRepositories
                  - ecr:PutLifecyclePolicy
                  - ecr:PutImageScanningConfiguration
                  - ecr:TagResource
                  - ecr:ListTagsForResource
                Resource: !Sub arn:aws:ecr:${AWS::Region}:${AWS::AccountId}:repository/${EnvironmentName}-*

              # ---- ECS/ALB/autoscaling: 04-ecs-alb.yaml. Same caveat as EC2 -
              # ELBv2/ECS/Application Auto Scaling create calls largely require
              # '*'. ----
              - Sid: EcsAlbAutoScaling
                Effect: Allow
                Action:
                  - ecs:CreateCluster
                  - ecs:DeleteCluster
                  - ecs:DescribeClusters
                  - ecs:CreateService
                  - ecs:UpdateService
                  - ecs:DeleteService
                  - ecs:DescribeServices
                  - ecs:RegisterTaskDefinition
                  - ecs:DeregisterTaskDefinition
                  - ecs:DescribeTaskDefinition
                  - ecs:TagResource
                  - elasticloadbalancing:CreateLoadBalancer
                  - elasticloadbalancing:DeleteLoadBalancer
                  - elasticloadbalancing:DescribeLoadBalancers
                  - elasticloadbalancing:CreateTargetGroup
                  - elasticloadbalancing:DeleteTargetGroup
                  - elasticloadbalancing:DescribeTargetGroups
                  - elasticloadbalancing:ModifyTargetGroup
                  - elasticloadbalancing:CreateListener
                  - elasticloadbalancing:DeleteListener
                  - elasticloadbalancing:DescribeListeners
                  - elasticloadbalancing:ModifyListener
                  - elasticloadbalancing:AddTags
                  - elasticloadbalancing:DescribeTags
                  - application-autoscaling:RegisterScalableTarget
                  - application-autoscaling:DeregisterScalableTarget
                  - application-autoscaling:DescribeScalableTargets
                  - application-autoscaling:PutScalingPolicy
                  - application-autoscaling:DeleteScalingPolicy
                  - application-autoscaling:DescribeScalingPolicies
                Resource: '*'
              # First-ever ScalableTarget in the account auto-creates this
              # service-linked role; the deploying principal needs explicit
              # permission to create it.
              - Sid: EcsAutoScalingServiceLinkedRole
                Effect: Allow
                Action: iam:CreateServiceLinkedRole
                Resource: !Sub arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
                Condition:
                  StringEquals:
                    iam:AWSServiceName: ecs.application-autoscaling.amazonaws.com

              # ---- CodePipeline/CodeDeploy/EventBridge: 05-cicd-pipeline.yaml ----
              - Sid: CodePipelineCodeDeployEventBridge
                Effect: Allow
                Action:
                  - codepipeline:CreatePipeline
                  - codepipeline:DeletePipeline
                  - codepipeline:GetPipeline
                  - codepipeline:UpdatePipeline
                  - codepipeline:TagResource
                  - codedeploy:CreateApplication
                  - codedeploy:DeleteApplication
                  - codedeploy:GetApplication
                  - codedeploy:CreateDeploymentGroup
                  - codedeploy:DeleteDeploymentGroup
                  - codedeploy:GetDeploymentGroup
                  - codedeploy:UpdateDeploymentGroup
                  - events:PutRule
                  - events:DeleteRule
                  - events:DescribeRule
                  - events:PutTargets
                  - events:RemoveTargets
                Resource: '*'
              # ArtifactBucket - CreateBucket/versioning/encryption are covered
              # here; PublicAccessBlockConfiguration and Tags on that bucket are
              # each a separate API action from CreateBucket and easy to miss.
              - Sid: ArtifactBucket
                Effect: Allow
                Action:
                  - s3:CreateBucket
                  - s3:DeleteBucket
                  - s3:PutBucketVersioning
                  - s3:PutEncryptionConfiguration
                  - s3:PutBucketPublicAccessBlock
                  - s3:PutBucketTagging
                  - s3:GetBucketLocation
                  - s3:ListBucket
                Resource: !Sub arn:aws:s3:::${EnvironmentName}-artifacts-*

              # ---- IAM roles created by 04/05/06/07 themselves ----
              - Sid: IamRolesForStacks
                Effect: Allow
                Action:
                  - iam:CreateRole
                  - iam:DeleteRole
                  - iam:GetRole
                  - iam:PutRolePolicy
                  - iam:DeleteRolePolicy
                  - iam:AttachRolePolicy
                  - iam:DetachRolePolicy
                  - iam:TagRole
                Resource: !Sub arn:aws:iam::${AWS::AccountId}:role/${EnvironmentName}-*

              # ---- iam:PassRole, scoped per role and per consuming service -
              # same pattern as 05-cicd-pipeline.yaml's PassEcsRoles statement.
              # No blanket Resource: '*' here. ----
              - Sid: PassEcsTaskRoles
                Effect: Allow
                Action: iam:PassRole
                Resource:
                  - !ImportValue
                    Fn::Sub: ${EnvironmentName}-TaskExecutionRoleArn
                  - !ImportValue
                    Fn::Sub: ${EnvironmentName}-TaskRoleArn
                Condition:
                  StringEquals:
                    iam:PassedToService: ecs-tasks.amazonaws.com
              - Sid: PassCodePipelineRole
                Effect: Allow
                Action: iam:PassRole
                Resource: !Sub arn:aws:iam::${AWS::AccountId}:role/${EnvironmentName}-codepipeline-role
                Condition:
                  StringEquals:
                    iam:PassedToService: codepipeline.amazonaws.com
              - Sid: PassCodeDeployRole
                Effect: Allow
                Action: iam:PassRole
                Resource: !Sub arn:aws:iam::${AWS::AccountId}:role/${EnvironmentName}-codedeploy-role
                Condition:
                  StringEquals:
                    iam:PassedToService: codedeploy.amazonaws.com
              - Sid: PassEventBridgeRole
                Effect: Allow
                Action: iam:PassRole
                Resource: !Sub arn:aws:iam::${AWS::AccountId}:role/${EnvironmentName}-eventbridge-pipeline-role
                Condition:
                  StringEquals:
                    iam:PassedToService: events.amazonaws.com

              # ---- Live task definition lookup used by deploy-cascade.sh
              # before touching 04-ecs-alb, so the cascade never reverts
              # whatever CodeDeploy has since promoted ----
              - Sid: LiveTaskDefinitionLookup
                Effect: Allow
                Action:
                  - ecs:DescribeServices
                  - ecs:DescribeTaskDefinition
                Resource: '*'
      Tags:
        - Key: Name
          Value: !Sub ${EnvironmentName}-infra-deploy-role

Outputs:
  InfraDeployRoleArn:
    Value: !GetAtt InfraDeployRole.Arn
    Export:
      Name: !Sub ${EnvironmentName}-InfraDeployRoleArn
